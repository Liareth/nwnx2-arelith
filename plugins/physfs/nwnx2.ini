; Archives are appended to the search path, so lower
; loadN will have a higher priority.
; Supports zip, 7z, a number of other package formats,
; see: http://icculus.org/physfs/
[PHYSFS]
load0 = physfs/ta_scripts.zip
;load1 = archive
;...
;loadN = archiveN
